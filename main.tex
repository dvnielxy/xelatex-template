% Versión: 1.0.1 (02/07/2019)
% Autor: Daniel Lobos (danieeelxy@gmail.com)

% ///////////////
% PREÁMBULO
% ///////////////

\documentclass[openany,oneside]{book}

% Path a archivos
%\makeatletter
%\def\input@path{{/path/}}
%\makeatother

% Instalación de paquetes
\input{lib/env/packages}

% Path de imágenes
\graphicspath{{./img/}}

% Colores y estilos
\input{lib/sty/colors}

% Estilo de títulos
\input{lib/sty/titles}

% Estilo de página (encabezados, pie de página, etc.)
\input{lib/sty/page}

% Estilo de fuentes
\input{lib/sty/fonts}

% Distancias, espaciados y espacios
\input{lib/sty/spacing}

% Comandos simbólicos
\input{lib/cmd/equation}
\input{lib/cmd/lists}
\input{lib/cmd/blankpage}
	% Nota: El comando \mathbb{...} brinda una tipografía distinta a la usual, pero soporta números y letras griegas mayúsculas; para la fuente usual, usar \amsmathbb{...}.
	% Nota: En un entorno matemático puedes usar los comandos \upalpha, \upbeta, ..., \upomega para usar letras griegas sin cursiva.
	% Nota: Esto último utiliza una fuente para estas letras que no viene instalada en el PC (fuente CMU Serif), se adjunta un .zip con la fuente para instalar.

% Comandos para TikZ
\input{lib/env/tikz}

% Configuración de entornos
\input{lib/env/environments}

% Configuración miscelánea
\input{lib/cfg/config}

% Configración inicial
%\autor{ }{ }
	% Nota: El primer parámetro recibe el nombre y el segundo una medida en centímetros, que es la altura desde donde comienza el nombre, e. g., \autor{Daniel Lobos}{1cm}.
%\titulo{ }
	% Nota: Aquí se incluye el título de tu documento, que quedará en cursiva en la parte inferior izquierda. Para que no diga nada, déjalo como \titulo{}.
%\setmainfont{ }
	% Nota: Aquí va la fuente principal, por defecto es \setmainfont{Latin Modern Roman}, pero puedes usar cualquier fuente que tengas instalada en tu PC.
%\setsansfont{ }
	% Nota: Aquí va la fuente sin serifas (no es la fuente de títulos). Esta fuente se activa mediante {\sffamily ...} o mediante \textsf{...}. 
	% Nota: Las fuentes de título son: Montserrat para títulos de capítulos y Corbel para otros títulos (por defecto, Montserrat no viene instalada, se adjunta un .zip con la fuente para instalar).
%\definecolor{bar1}{RGB}{x,y,z}
%\definecolor{bar2}{RGB}{x,y,z}
	% Nota: Los valores de x, y y z son valores entre 0 y 255.
\barheight{5cm}
	% Nota: Aquí se debe incluir una altura. La hoja que se usa por defecto es A4, por lo que la altura debe estar entre 0cm y 29.7cm.

% ///////////////////////////////
% INICIO DEL DOCUMENTO
% ///////////////////////////////

\begin{document}
asdasd
\end{document}